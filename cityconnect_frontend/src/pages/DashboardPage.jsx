import React from "react";
import Dashboard from "../components/Dashboard";

// PUBLIC_INTERFACE
/**
 * DashboardPage - wraps authority dashboard (protected).
 */
const DashboardPage = () => (
  <div style={{ marginTop: "4rem" }}>
    <Dashboard />
  </div>
);

export default DashboardPage;
